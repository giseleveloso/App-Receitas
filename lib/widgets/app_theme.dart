import 'package:flutter/material.dart';

class AppTheme {
  static const Color laranja = Color(0xFFFF9500);         // cor principal (botões, ícones ativos)
  static const Color laranjaClaro = Color(0xFFFFF3E0);    // fundo suave para chips e placeholders
  static const Color cinzaFundo = Color(0xFFF5F5F5);      // fundo geral das telas
  static const Color cinzaTexto = Color(0xFF888888);      // textos secundários e ícones inativos
  static const Color branco = Colors.white;               // cards, headers e campos
  static const Color preto = Color(0xFF1A1A1A);           // textos principais
}
